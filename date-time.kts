import java.time.Year
import java.util.*

fun main(){
    //Still exploring
    print("The date Today is:")
    var date=Calendar.getInstance()
    var today=date.get(Calendar.DATE)
    var month=date.get(Calendar.MONTH)
    var year=date.get(Calendar.YEAR)
    month +=1
    println(date)
    println("$today/$month/$year")


















}
main()